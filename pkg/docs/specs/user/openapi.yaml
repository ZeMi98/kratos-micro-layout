# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Kratos Micro Layout API
    description: HTTP API generated from api/**.proto. Bodies are protojson and every reply is wrapped in a code/message/data envelope.
    version: 1.0.0
servers:
    - url: https://user.example.com
paths:
    /v1/auth/change-password:
        post:
            tags:
                - AuthService
            description: ChangePassword updates the authenticated user's password.
            operationId: AuthService_ChangePassword
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/user.v1.ChangePasswordRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/user.v1.ChangePasswordResponse'
    /v1/auth/login:
        post:
            tags:
                - AuthService
            description: Login authenticates a user and returns tokens.
            operationId: AuthService_Login
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/user.v1.LoginRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/user.v1.LoginResponse'
    /v1/auth/logout:
        post:
            tags:
                - AuthService
            description: Logout invalidates the current session.
            operationId: AuthService_Logout
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/user.v1.LogoutRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/user.v1.LogoutResponse'
    /v1/auth/refresh:
        post:
            tags:
                - AuthService
            description: RefreshToken exchanges a valid refresh token for a new token pair.
            operationId: AuthService_RefreshToken
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/user.v1.RefreshTokenRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/user.v1.RefreshTokenResponse'
    /v1/auth/register:
        post:
            tags:
                - AuthService
            description: Register creates a new user account with credentials.
            operationId: AuthService_Register
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/user.v1.RegisterRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/user.v1.RegisterResponse'
    /v1/users/profile:
        get:
            tags:
                - UserService
            description: GetProfile returns the caller's own account.
            operationId: UserService_GetProfile
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/user.v1.GetProfileResponse'
        patch:
            tags:
                - UserService
            description: |-
                UpdateProfile patches the caller's own profile fields. Identity fields
                 (username, email) and server-assigned fields are not patchable here.
            operationId: UserService_UpdateProfile
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/user.v1.UpdateProfileRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/user.v1.UpdateProfileResponse'
components:
    schemas:
        user.v1.ChangePasswordRequest:
            required:
                - old_password
                - new_password
            type: object
            properties:
                old_password:
                    type: string
                    description: Current password for verification.
                new_password:
                    type: string
                    description: New password. Must be 8-72 characters (bcrypt caps input at 72 bytes).
            description: ChangePasswordRequest is the request for ChangePassword.
        user.v1.ChangePasswordResponse:
            type: object
            properties: {}
            description: ChangePasswordResponse is the response for ChangePassword.
        user.v1.GetProfileResponse:
            type: object
            properties:
                user:
                    allOf:
                        - $ref: '#/components/schemas/user.v1.User'
                    description: The caller's own account.
            description: GetProfileResponse is the response message for GetProfile.
        user.v1.LoginRequest:
            required:
                - username
                - password
            type: object
            properties:
                username:
                    type: string
                    description: Login username.
                password:
                    type: string
                    description: Plain-text password.
            description: LoginRequest is the request for Login.
        user.v1.LoginResponse:
            type: object
            properties:
                tokens:
                    $ref: '#/components/schemas/user.v1.TokenPair'
                user_id:
                    type: string
                    description: User ID (snowflake int64; JSON string via protojson).
                username:
                    type: string
                    description: Login username.
            description: LoginResponse is the response for Login.
        user.v1.LogoutRequest:
            type: object
            properties:
                access_token:
                    type: string
                    description: |-
                        The access token to invalidate. Optional: when omitted, the token is taken
                         from the Authorization header, which the auth middleware already validates.
            description: LogoutRequest is the request for Logout.
        user.v1.LogoutResponse:
            type: object
            properties: {}
            description: LogoutResponse is the response for Logout.
        user.v1.RefreshTokenRequest:
            required:
                - refresh_token
            type: object
            properties:
                refresh_token:
                    type: string
                    description: A valid refresh token.
            description: RefreshTokenRequest is the request for RefreshToken.
        user.v1.RefreshTokenResponse:
            type: object
            properties:
                tokens:
                    $ref: '#/components/schemas/user.v1.TokenPair'
            description: RefreshTokenResponse is the response for RefreshToken.
        user.v1.RegisterRequest:
            required:
                - username
                - email
                - password
            type: object
            properties:
                username:
                    type: string
                    description: Login username. Must be unique.
                email:
                    type: string
                    description: Email address. Must be unique.
                password:
                    type: string
                    description: Plain-text password. Must be 8-72 characters (bcrypt caps input at 72 bytes).
                nickname:
                    type: string
                    description: Display name (optional).
                phone:
                    type: string
                    description: Phone number (optional).
            description: |-
                RegisterRequest is the request for Register.

                 Field constraints are written as CEL rules with explicit `message` texts so
                 a rejected request tells the client exactly what to fix (the protovalidate
                 middleware surfaces them verbatim in a 400). Length rules double as presence
                 checks: a rule that fails on "" makes a separate `required` violation
                 redundant.
        user.v1.RegisterResponse:
            type: object
            properties:
                user_id:
                    type: string
                    description: ID of the newly created user (snowflake int64; JSON string via protojson).
                username:
                    type: string
                    description: Login username.
                email:
                    type: string
                    description: Email address.
            description: RegisterResponse is the response for Register.
        user.v1.TokenPair:
            type: object
            properties:
                access_token:
                    type: string
                    description: Short-lived access token (JWT, default 2h).
                refresh_token:
                    type: string
                    description: Long-lived refresh token (JWT, default 7d).
                expires_in:
                    type: string
                    description: Access token TTL in seconds.
                token_type:
                    type: string
                    description: Token type. Always "Bearer".
            description: TokenPair is a pair of access and refresh tokens.
        user.v1.UpdateProfileRequest:
            type: object
            properties:
                phone:
                    type: string
                    description: Phone number (optional).
                nickname:
                    type: string
                    description: Display name.
                avatar:
                    type: string
                    description: Avatar URL.
                update_mask:
                    type: string
                    description: |-
                        Field mask over the patchable profile fields: phone, nickname, avatar.
                         Selecting a field writes it (an empty value clears it); unselected fields
                         are left alone.
                    format: field-mask
            description: |-
                UpdateProfileRequest is the request message for UpdateProfile. Only the
                 profile fields are patchable; update_mask selects which of them to apply. A
                 selected field is written with whatever value the request carries — leaving it
                 empty clears it — while an unselected field keeps its stored value.
                 Constraints are CEL rules with explicit `message` texts so a rejected request
                 tells the client exactly what to fix (the protovalidate middleware surfaces
                 them in a 400); IGNORE_IF_ZERO_VALUE keeps an unselected field's zero value
                 from tripping them.
        user.v1.UpdateProfileResponse:
            type: object
            properties:
                user:
                    allOf:
                        - $ref: '#/components/schemas/user.v1.User'
                    description: The updated account.
            description: UpdateProfileResponse is the response message for UpdateProfile.
        user.v1.User:
            required:
                - username
                - email
            type: object
            properties:
                id:
                    readOnly: true
                    type: string
                    description: |-
                        Unique identifier (snowflake int64). Serialised as a JSON string by
                         protojson so the 64-bit value stays precise in JavaScript clients.
                username:
                    type: string
                    description: Login username. Unique across the system; assigned at registration.
                email:
                    type: string
                    description: Email address. Unique across the system; assigned at registration.
                phone:
                    type: string
                    description: Phone number (optional).
                nickname:
                    type: string
                    description: Display name.
                avatar:
                    type: string
                    description: Avatar URL.
                status:
                    readOnly: true
                    enum:
                        - USER_STATUS_UNSPECIFIED
                        - USER_STATUS_ACTIVE
                        - USER_STATUS_DISABLED
                        - USER_STATUS_DELETED
                    type: string
                    description: Lifecycle status. Server-assigned; reads filter out deleted accounts.
                    format: enum
                created_at:
                    readOnly: true
                    type: string
                    description: Server-assigned creation timestamp.
                    format: date-time
                updated_at:
                    readOnly: true
                    type: string
                    description: Server-assigned last-update timestamp.
                    format: date-time
            description: |-
                User is the user account resource. It is read-only on the wire: writes go
                 through UpdateProfile (profile fields) and AuthService (credentials), so the
                 resource carries no validation rules of its own.
tags:
    - name: AuthService
      description: AuthService handles registration, login, and token management.
    - name: UserService
      description: |-
        UserService serves the authenticated caller's own account. This template has
         no RBAC and no admin surface: accounts are created through
         AuthService.Register, so every RPC here is self-scoped — the caller identity
         comes from the verified bearer token, never from the request.
